<h1> BLURT OF THE DAY! </h1>


<div class="best-blurt">
   <h3> <%= simple_format(@best_blurt.content)  %></h3>
   <h4> Congratulations <%= User.find(@best_blurt.user_id).username %>! </h4>
   <h5> <%= User.find(@best_blurt.user_id).username %> received  <%= @vote_quantity %> votes. </h5>
   <h5> It's approval rating is <%= @best_blurt.approval %> % </h5>
</div>
<h2> Some more great blurts </h2>

<% @blurts.each do |b| %>
<div class="blurt">
  <p> <%= simple_format(b.content) %> by <em> <%= b.user.username %> </em><p>
  <p> <%= b.approval %>% approval with <%= b.upvotes.length %> upvotes </p>
</div>
<% end %>
